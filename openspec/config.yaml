# openspec/config.yaml — SDD project configuration
# Auto-generated by sdd-init on 2026-05-27
schema: spec-driven

context: |
  Tech stack: Java 25, Spring Boot 4.0.6, PostgreSQL 18.3, Gradle Kotlin DSL
  Architecture: Hexagonal (Ports & Adapters), modular monolith by subdomain
  Package: com.logossystemsit.logiceducore
  Persistence: Spring Data JPA + Flyway (migrations pending)
  Security: Spring Security + JWT (pending)
  Source layout: src/main/java (hexagonal layers), src/test/java

testing:
  strict_tdd: true
  runner: JUnit 5
  test_command: ./gradlew test
  build_command: ./gradlew build
  layers:
    unit: JUnit 5 (available)
    integration: Spring Boot test slices (available)
    e2e: Not configured
  coverage: Not configured
  quality:
    linter: Not configured
    formatter: Not configured
    type_checker: Not applicable (Java compiler)

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    - Follow Hexagonal Architecture dependency rules
    tdd: true
    test_command: ./gradlew test --no-daemon
  verify:
    test_command: ./gradlew test
    build_command: ./gradlew build
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
