spring:
  application:
    name: logic-edu-core

  profiles:
    active: local

  jpa:
    properties:
      hibernate:
        ddl-auto: none
        format_sql: true

jwt:
  secret: ${JWT_SECRET}
  expiration-ms: ${JWT_EXPIRATION_MS:3600000}
