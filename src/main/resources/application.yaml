spring:
  application:
    name: logic-edu-core

  profiles:
    active: local

  jpa:
    open-in-view: false
    properties:
      hibernate:
        ddl-auto: none
        format_sql: true

jwt:
  secret: ${JWT_SECRET}
  expiration-ms: ${JWT_EXPIRATION_MS:3600000}
